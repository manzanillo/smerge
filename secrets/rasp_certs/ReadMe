# Important
Add pubilc and privat keys for nginx and your dns entry here / create a soft link if you use certbot and auto renewal.
If you don't want to change the nginx setups call the public key fullchain.pem and the privat key privkey.pem.